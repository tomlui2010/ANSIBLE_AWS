---
 # Exercise for setting up a web server in an EC
 - name: Exercise for setting up a prometheus server in an EC 
   hosts: prometheus_server
   become: true
   become_method: sudo
   become_user: root
   roles:
    - common
    - prometheus