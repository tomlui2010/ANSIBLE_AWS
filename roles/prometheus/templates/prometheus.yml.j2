global:
  scrape_interval: 1s
  evaluation_interval: 1s
  external_labels:
    monitor: 'prometheus'

scrape_configs:
  - job_name: 'node'
    static_configs:
      - targets:
        - {{ prometheus_target }}:9100